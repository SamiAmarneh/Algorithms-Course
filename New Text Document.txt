git config --global user.name ===> to set a username
git config --global user.email ===> to set an email address

git init ===> Initializes a new empty Git repository. If you want to place a project under vision control, this is the first command you need.
git add ===> adds a change in the working directory to the staging area. In conjunction with these commands.
git status ===> to view the state of the working directory and the staging area.
git commit ===> Takes the staged snapshot and commits it to the project history. 
    Syntax:-  git commit -m "the commit message"
 

git push ===> The git push command in Git is used to upload your local repository's commits to a remote repository. 
    git push <Remote Name> <Branch Name>` ⇒ Push the specified branch, along with all of the necessary commits and internal objects. For example, git push origin master.
